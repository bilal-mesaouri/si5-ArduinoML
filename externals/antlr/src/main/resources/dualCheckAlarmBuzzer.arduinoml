
application dualCheckAlarmBuzzer

# Declaring bricks
PinSensor buttonA: 9
PinSensor buttonB: 10
actuator buzzer: 12
# Declaring statesmessage
on {
    buzzer <= HIGH
    ( buttonA is HIGH AND buttonB is HIGH ) => off
}

-> off {
    buzzer <= LOW
    ( buttonA is LOW OR buttonB is LOW) => on
}
